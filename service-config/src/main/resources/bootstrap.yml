server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alarcon-osorio/MicroServices2022.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
    security:
      user:
        name: root
        password: S3cr3t
